## Zadanie 1 — Szkielet projektu (DONE)

Co zrobiłem:
- pyproject.toml: build-backend hatchling, deps (pydantic>=2, pandas, pyarrow),
  dev group (pytest, mypy, ruff), [tool.mypy] strict = true + override
  ignore_missing_imports dla pandas.*/pyarrow.*, [tool.ruff] line-length=100.
- src/feed_cleaner/__init__.py — pusty pakiet.
- tests/test_placeholder.py — jeden test importu pakietu.

Decyzje:
- ruff: dodałem `extend-exclude = ["scripts", "data"]`. Bez tego `ruff check .`
  łapał E501 w scripts/generate_synthetic_feed.py (pre-existing generator
  danych syntetycznych, per CLAUDE.md celowo POZA pętlą Ralpha — nie jest
  w files.include/exclude żadnego zadania w PRD.json). Zamiast ruszać ten
  plik, wykluczyłem scripts/ i data/ z zakresu ruff — lintujemy tylko kod
  pakietu (src/, tests/).
- Zweryfikowałem ręcznie (plik w scratchpad, nie w repo), że
  ignore_missing_imports faktycznie działa dla pandas/pyarrow pod
  mypy --strict, mimo że __init__.py na razie ich nie importuje.

Stop condition — wszystkie 4 komendy przeszły osobno:
1. uv sync — OK
2. uv run pytest — 1 passed
3. uv run mypy src — Success, no issues
4. uv run ruff check . — All checks passed
5. uv run python -c "import feed_cleaner" — OK

Do dokończenia: nic — zadanie 1 zamknięte. Następne w kolejce: zadanie 2
(schema Pydantic + parsing.py — parse_price/parse_quantity).

## Zadanie 2 — Schema Pydantic + parse_price/parse_quantity (DONE)

Co zrobiłem:
- src/feed_cleaner/models.py: Status (StrEnum: CLEAN/REPAIRED/REJECTED),
  Reason (StrEnum, wszystkie 11 nazwanych powodów z zadania 6 — zdefiniowane
  raz centralnie, część nieużywana jeszcze w tym zadaniu: duplicate_sku,
  malformed_row), FieldOutcome[T] (frozen dataclass generyczny: status,
  value, reasons=[]), ProductRecord (pydantic BaseModel — docelowy CZYSTY
  rekord: sku/name/price/quantity wymagane, category/currency opcjonalne;
  field_validator odrzuca puste/białoznakowe sku i name, normalizuje pustą
  kategorię do None).
- src/feed_cleaner/parsing.py: parse_price(raw) i parse_quantity(raw), obie
  zwracają FieldOutcome. parse_price zwraca PriceValue (NamedTuple:
  price+currency) — waluta jest wyłącznie wyekstrahowana z tekstu ceny
  (generator nie ma osobnej kolumny currency w CSV).
- tests/test_models.py, tests/test_parsing.py — 31 testów łącznie, każdy
  edge_case z PRD pokryty + jeden dodatkowy (case-insensitive currency
  matching, "Usd" -> USD) na sugestię reviewera.

Decyzje architektoniczne:
- FieldOutcome to zwykły `@dataclass(frozen=True, Generic[T])`, NIE model
  pydantic — pydantic-generics wymagałby pluginu mypy (`pydantic.mypy`),
  a pyproject.toml jest POZA scope zadania 2 (nie ma go w files.include).
  Dataclass generyczny działa poprawnie pod mypy --strict bez dodatkowej
  konfiguracji. ProductRecord zostaje pydantic BaseModel — to jedyny
  fragment PRD, który explicite tego wymaga.
- WAŻNE dla zadania 3 (classify.py): walidacja pustego sku/name żyje
  WYŁĄCZNIE jako field_validator na ProductRecord (rzuca ValueError z
  Reason.MISSING_SKU/MISSING_NAME.value w treści) — w parsing.py NIE MA
  odpowiednika parse_sku/parse_name (PRD explicite nazywa tylko
  parse_price/parse_quantity jako wymagane funkcje). Ale ProductRecord
  wymaga price:float i quantity:int (nie-opcjonalne) — nie da się go
  skonstruować, żeby sprawdzić samo sku, jeśli price/quantity danego
  wiersza jest jednocześnie niepoprawne. classify.py NIE będzie więc mógł
  polegać na ProductRecord do wykrywania missing_sku/missing_name przy
  kompozycji wiersza (potrzebuje reason nawet gdy inne pola są REJECTED).
  Rekomendacja: classify.py powinien mieć własny, lokalny, jednowierszowy
  helper (`if not sku.strip(): FieldOutcome(REJECTED, None, [Reason.MISSING_SKU])`)
  zdefiniowany we własnym pliku, korzystając z FieldOutcome/Status/Reason
  zaimportowanych z models.py (import jest dozwolony, edycja pliku już nie —
  models.py/parsing.py są poza files.include zadania 3). ProductRecord i
  jego walidatory zostają jako safety net przy finalnej konstrukcji rekordu
  w eksporcie (zadanie 5), gdzie sku/name są już wtedy pewne, że są OK.
- Ekstrakcja waluty (_extract_currency w parsing.py): dopasowanie
  prefiks/sufiks przez proste startswith/endswith (case-insensitive) na
  liście tokenów (zł/pln/$/usd/€/eur), bez regex — feed syntetyczny nie ma
  przypadków wieloznacznych (np. separator tysięcy). Kolejność REJECTED >
  REPAIRED jest rozstrzygana na końcu: wykrycie waluty/przecinka samo w
  sobie nie nadpisuje reguły <=0 -> invalid_price.

Stop condition — cały feedback loop zielony:
1. uv run pytest — 31 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed

Do dokończenia: nic — zadanie 2 zamknięte. Następne w kolejce: zadanie 3
(classify.py — kompozycja statusu wiersza + dedup SKU), patrz notatka
architektoniczna wyżej przed startem.

## Zadanie 3 — classify.py: kompozycja statusu wiersza + dedup SKU (DONE)

Co zrobiłem:
- parsing.py: dopisane parse_sku(raw) i parse_name(raw), symetryczne do
  parse_price/parse_quantity — puste/białoznakowe/None -> REJECTED
  missing_sku/missing_name; niepuste -> CLEAN, wartość po .strip().
- src/feed_cleaner/classify.py (nowy plik): ClassifiedRow (frozen dataclass:
  wyniki pięciu pól + status + reasons całego wiersza), classify_row(raw)
  i deduplicate_by_sku(rows).
- tests/test_classify.py (nowy plik, 10 testów) + rozszerzenie
  test_parsing.py o TestParseSku/TestParseName. Łącznie 52 testy.

Decyzje architektoniczne:
- classify_row NIE konstruuje ProductRecord — woła bezpośrednio
  parse_sku/parse_name/parse_price/parse_quantity + lokalny _parse_category
  (zawsze Status.CLEAN, wartość None dla pustego/braku — bez własnej funkcji
  parse_*, zgodnie z PRD). Dzięki temu każde pole liczy się NIEZALEŻNIE, także
  gdy inne pole jest jednocześnie zepsute (czego ProductRecord by nie
  pozwolił — nie da się skonstruować modelu z częściowo złymi polami).
- Reguła kompozycji: reasons wiersza = konkatenacja reasons WSZYSTKICH pięciu
  pól w stałej kolejności (sku, name, price, quantity, category), niezależnie
  od gałęzi statusu — bo CLEAN i tak ma reasons=[]. Status: REJECTED jeśli
  którekolwiek pole REJECTED, inaczej REPAIRED jeśli którekolwiek REPAIRED,
  inaczej OK. Rozstrzygnięcie pozornej sprzeczności w PRD (opis zadania 3 mówi
  "reasons REJECTED = suma reason z pól REJECTED", ale edge_case mówi wprost
  "REJECTED+REPAIRED -> reasons zawiera OBA"): przyjąłem wariant zgodny z
  edge_case (reasons zawiera też reasons z pól REPAIRED, gdy wiersz i tak jest
  REJECTED) — potwierdzone przez advisora, dodatkowo pokryte testem
  test_one_rejected_field_and_one_repaired_field_is_rejected_with_both_reasons.
- deduplicate_by_sku: osobna funkcja, osobny przebieg PO classify_row (nie
  wewnątrz), keep-first w kolejności listy wejściowej. Klucz dedup to
  row.sku.value (czyli już zparsowany, po strip) — wiersze z sku.value=None
  (odrzucone jako missing_sku) pomijane w dedup i pomiędzy sobą. Duplikat:
  cały wiersz -> REJECTED, DUPLICATE_SKU DOPISANY (nie zastępujący) do
  istniejących reasons — pokryte osobnym testem
  test_duplicate_sku_is_appended_to_existing_reasons_not_replacing_them.
  Pierwsze wystąpienie NIGDY nie jest ruszane przez dedup, nawet jeśli samo
  jest REJECTED z innego powodu (np. zły price) — trafia do `seen` i zostaje
  ze swoim oryginalnym reason.
- RawRow = Mapping[str, str | None] (collections.abc, zgodnie z ruff UP035) —
  luźny typ wejścia dla classify_row, żeby loader.py (zadanie 4) mógł podawać
  surowe wiersze CSV bez konieczności budowania osobnego typu.

Stop condition — cały feedback loop zielony:
1. uv run pytest — 52 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed

Do dokończenia: nic — zadanie 3 zamknięte. Następne w kolejce: zadanie 4
(loader.py — wczytanie CSV z obsługą BOM/malformed_row + spięcie z
classify_row/deduplicate_by_sku, zachowanie kolejności wejściowej).

## Zadanie 4 — loader.py: wczytanie CSV + spięcie z classify/dedup (DONE)

Co zrobiłem:
- src/feed_cleaner/loader.py (nowy plik): load_feed(path) — otwiera plik
  z encoding="utf-8-sig" (obsługuje BOM, ale też pliki bez BOM), czyta
  csv.reader (nie DictReader — potrzebowałem surowej liczby kolumn w
  wierszu do porównania z nagłówkiem, DictReader to chowa w restkey/
  restval). Dla każdego wiersza danych: jeśli len(raw_row) != len(header)
  -> _malformed_row() (REJECTED wprost, z pominięciem całego pipeline'u
  parsing/classify); inaczej dict(zip(header, raw_row, strict=True)) i
  classify_row(record) z zadania 3. Na końcu deduplicate_by_sku(rows) na
  całej liście (dedup musi widzieć wszystkie wiersze łącznie z malformed,
  żeby kolejność/keep-first było poprawne względem pliku).
- tests/test_loader.py (nowy plik, 7 testów, tmp_path + zapis bajtów przez
  write_bytes żeby kontrolować dokładne encoding/BOM) — pokrywa wszystkie
  edge_cases z PRD: header-only -> [], BOM -> wczytuje się, nieznana
  dodatkowa kolumna ignorowana, wiersz z za małą/za dużą liczbą kolumn ->
  malformed_row + reszta pliku normalnie, kolejność zachowana, dedup
  działa też gdy w pliku są malformed rows przed duplikatem.

Decyzje architektoniczne:
- _malformed_row() buduje ClassifiedRow z pięcioma polami REJECTED/value=None
  BEZ żadnego reason na poziomie pola (nie ma wiarygodnych granic pól, więc
  nie przypisuję reason do konkretnego pola) — Reason.MALFORMED_ROW ląduje
  wyłącznie na poziomie row.reasons, analogicznie do tego jak
  deduplicate_by_sku dopisuje DUPLICATE_SKU na poziomie wiersza, nie pola.
  To zostawia zadaniu 6 (report.py) decyzję, jak liczyć malformed_row w
  raporcie — PRD zadania 6 nie precyzuje tego wprost (mówi tylko o pięciu
  polach + osobno duplicate_sku), więc nie przesądzałem tego tutaj.
- Użyłem csv.reader zamiast csv.DictReader — porównanie liczby kolumn
  wprost (len(raw_row) != len(header)) jest jawne i czytelne; DictReader
  wymagałby sprawdzania obecności kluczy restkey/None i restval/None, co
  jest mniej czytelne dla tej samej logiki.
- zip(header, raw_row, strict=True) bezpieczne, bo w tym miejscu kod już
  wie że długości się zgadzają (sprawdzone linijkę wyżej) — strict=True
  tylko jako asercja, nigdy realnie nie rzuci.
- Puste linie w CSV (csv.reader zwraca [] dla pustej linii) trafiają w tę
  samą gałąź co malformed_row (len([]) != column_count) — PRD nie
  wyróżnia pustych linii jako osobnego przypadku, więc nie dodawałem
  specjalnej obsługi.

Stop condition — cały feedback loop zielony:
1. uv run pytest — 59 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed

Do dokończenia: nic — zadanie 4 zamknięte. Następne w kolejce: zadanie 5
(export.py — zapis OK+REPAIRED do Parquet przez pyarrow, schema typów,
pusty plik z poprawnym schematem gdy 0 zaakceptowanych rekordów).

## Zadanie 5 — export.py: zapis OK+REPAIRED do Parquet (DONE)

Co zrobiłem:
- src/feed_cleaner/export.py (nowy plik): export_to_parquet(rows, output_path)
  — filtruje ClassifiedRow do status != REJECTED, buduje pa.Table z jawnym
  SCHEMA (sku/name/price/quantity nullable=False, category/currency
  nullable=True; price=float64, quantity=int64) i zapisuje przez
  pyarrow.parquet.write_table. Tworzy katalog nadrzędny (mkdir parents=True,
  exist_ok=True) przed zapisem.
- tests/test_export.py (nowy plik, 6 testów) — pokrywa wszystkie edge_cases
  z PRD: 0 zaakceptowanych rekordów -> poprawny pusty plik z tym samym
  schematem (SCHEMA.equals), wartości w Parquet to wartości PO naprawie
  (np. "29,99 zł" -> 29.99 + currency="PLN", "-5" -> 5), katalog wyjściowy
  nieistniejący (zagnieżdżony) -> tworzony automatycznie, category/currency
  None -> zapisywane jako null bez crasha. Dodatkowo: REJECTED rows pomijane
  całkowicie, pusta lista wejściowa -> też poprawny pusty plik.

Decyzje architektoniczne:
- currency NIE ma własnej kolumny w ClassifiedRow — pochodzi z
  row.price.value.currency (PriceValue z parsing.py, ekstrahowana z tekstu
  ceny w zadaniu 2), zgodnie z notatką z zadania 2 ("waluta jest wyłącznie
  wyekstrahowana z tekstu ceny, generator nie ma osobnej kolumny currency").
- Jawny pa.schema() zamiast polegania na inferencji typów przez
  pa.table()/pandas — kluczowe dla edge case'u "0 rekordów": bez jawnego
  schematu puste listy nie dają pyarrow wystarczającej informacji o typie
  docelowym (dostałby null/na type zamiast float64/int64 itd.).
- sku/name/price/quantity.value dla wierszy status!=REJECTED są z
  konstrukcji parsingu (zadanie 2/3) zawsze not-None (REJECTED to jedyny
  status, który zostawia value=None) — narzucone przez `assert` (nie
  if/raise), bo to invariant pipeline'u, nie walidacja danych wejściowych z
  zewnątrz; assert też pozwala mypy --strict zawęzić typ z `T | None` do `T`
  bez dodatkowego kodu.

Stop condition — cały feedback loop zielony:
1. uv run pytest — 65 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed

Do dokończenia: nic — zadanie 5 zamknięte. Następne w kolejce: zadanie 6
(report.py — agregacja liczników row_counts/repaired_reasons/rejected_reasons
do JSON, reguła "licznik z pięciu pól ClassifiedRow, nie z płaskiej listy
row.reasons" opisana w PRD zadania 6).

## Zadanie 6 — report.py: agregacja liczników do JSON + skrót tekstowy (DONE)

Uruchomione w trybie AFK (bezobsługowym) — bez pytań o zgodę, zgodnie z
punktem 7 sekcji HITL w CLAUDE.md.

Co zrobiłem:
- src/feed_cleaner/report.py (nowy plik): QualityReport (frozen dataclass:
  row_counts/repaired_reasons/rejected_reasons + to_dict()), build_report(rows),
  write_report(report, output_path) — zapis JSON z mkdir(parents=True), i
  format_summary(report) — jednolinijkowy skrót do stdout (zadanie 7 użyje).
- tests/test_report.py (nowy plik, 9 testów) — pokrywa wszystkie edge_cases
  z PRD zadania 6 + dodatkowe testy write_report/format_summary.

Decyzje architektoniczne:
- repaired_reasons i rejected_reasons to DWA OSOBNE dict, każdy z kluczami dla
  WSZYSTKICH 11 znanych Reason (nie tylko tych, które mogą realnie wystąpić w
  danej kategorii) — najbardziej literalna interpretacja PRD ("wszystkie znane
  reason obecne w wyniku nawet z count=0"), upraszcza ręczną rekoncyliację
  (można zawsze odpytać ten sam klucz w obu dictach bez sprawdzania, czy
  istnieje).
- Reguła liczenia z pięciu pól: dla każdego wiersza REPAIRED/REJECTED,
  iteruję po (sku, name, price, quantity, category) i liczę reason z pola
  TYLKO gdy outcome.status == row.status (czyli pole REJECTED liczy się
  wyłącznie gdy wiersz jest REJECTED; pole REPAIRED tylko gdy wiersz jest
  REPAIRED). To automatycznie realizuje regułę z PRD "reason z pola REPAIRED
  na wierszu ostatecznie REJECTED nie liczy się nigdzie" — bez dodatkowego
  warunku, bo status pola i status wiersza się wtedy nie zgadzają.
- ROZSTRZYGNIĘCIE DECYZJI zostawionej otwartej w zadaniu 4 (loader.py, nie
  ma reason na poziomie pola dla malformed_row — cały pipeline pól jest
  pomijany): potraktowałem Reason.MALFORMED_ROW analogicznie do
  Reason.DUPLICATE_SKU — obie te wartości NIE należą do żadnego z pięciu pól
  (żyją wyłącznie w row.reasons na poziomie wiersza, dopisywane przez inne
  etapy pipeline'u: loader dla malformed_row, deduplicate_by_sku dla
  duplicate_sku), więc liczę je wprost do rejected_reasons, gdy obecne w
  row.reasons wiersza REJECTED. Bez tego malformed_row nigdy nie pojawiłoby
  się w żadnym liczniku raportu mimo że realnie zdecydowało o odrzuceniu
  wiersza — sprzeczne z celem z CLAUDE.md ("ile rekordów odrzucono... wg
  jakiej reguły"). PRD zadania 6 wymienia explicite tylko duplicate_sku jako
  wyjątek row-level, ale nie ma żadnego powodu architektonicznego, żeby
  malformed_row potraktować inaczej — ten sam mechanizm (informacja żyje na
  wierszu, nie na polu) stosuje się do obu. Potwierdzone testem
  test_malformed_row_counted_in_rejected_reasons. Generator syntetycznego
  feedu (scripts/generate_synthetic_feed.py) i tak celowo NIE wstrzykuje
  malformed_row (komentarz w kodzie: "raport ma je pokazywać z count=0, nie
  pomijać") — w praktyce ta gałąź nie odpali się na danych produkcyjnych z
  tego sprintu, ale kod jest poprawny ogólnie, nie tylko dla tego jednego
  manifestu.
- _field_outcomes(row) zwraca tuple[FieldOutcome[Any], ...] (typing.Any, nie
  object) — bo FieldOutcome[str] i FieldOutcome[PriceValue] nie są zgodne z
  FieldOutcome[object] pod mypy --strict (Generic[T] jest niekowariantny),
  a wewnątrz build_report używam tylko .status/.reasons, nie .value, więc
  Any jest tu bezpieczne i nie ukrywa żadnego realnego błędu typów.

Stop condition — cały feedback loop zielony:
1. uv run pytest — 74 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed

Do dokończenia: nic — zadanie 6 zamknięte. Następne w kolejce: zadanie 7
(cli.py — argparse, spięcie loader -> classify -> export -> report, entry
point [project.scripts] w pyproject.toml).

## Zadanie 7 — cli.py: argparse + spięcie loader/classify/export/report (DONE)

Uruchomione w trybie AFK (bezobsługowym) — bez pytań o zgodę, zgodnie z
punktem 7 sekcji HITL w CLAUDE.md.

Co zrobiłem:
- src/feed_cleaner/cli.py (nowy plik): _build_parser() (argparse, --input
  wymagane Path, --output-dir domyślnie Path("output")), main(argv) -> int.
  Sprawdza input_path.is_file() przed jakąkolwiek pracą — jeśli nie istnieje,
  czytelny komunikat na stderr (bez tracebacku) i return EXIT_USAGE_ERROR (2).
  W przeciwnym razie: load_feed -> export_to_parquet(output_dir/clean.parquet)
  -> build_report -> write_report(output_dir/report.json) -> print(format_summary)
  -> return EXIT_OK (0), niezależnie od tego, ile wierszy zostało odrzuconych
  (to normalne zachowanie danych, nie błąd CLI, zgodnie z PRD).
- pyproject.toml: dodane [project.scripts] feed-cleaner = "feed_cleaner.cli:main".
- tests/test_cli.py (nowy plik, 5 testów, wywołują main() bezpośrednio z listą
  argv zamiast subprocess — szybsze, prostsze do debugowania) — pokrywa
  wszystkie edge_cases z PRD: nieistniejący --input -> stderr bez "Traceback",
  exit code != 0; nieistniejący --output-dir -> tworzony automatycznie (przez
  mkdir w export.py/report.py, cli.py nie musi nic dodatkowo robić); plik tylko
  z nagłówkiem -> exit 0, parquet 0 wierszy, report.json total=0; sukces z
  częścią odrzuconych rekordów -> exit 0; pełny przebieg end-to-end tworzy oba
  pliki wyjściowe.

Decyzje architektoniczne:
- Walidacja istnienia pliku wejściowego zrobiona jawnie w main() przez
  input_path.is_file() PRZED wywołaniem load_feed, zamiast łapania wyjątku
  z loader.py (który używa path.open()) — celowo, żeby (a) nie dotykać
  loader.py (poza scope zadania 7 co do reguł biznesowych, choć plik jest
  formalnie w files.include tylko dla cli.py/pyproject.toml/test_cli.py, więc
  loader.py editować nie wolno), (b) komunikat błędu być przewidywalny i nie
  zależeć od tego, czy load_feed rzuci FileNotFoundError czy IsADirectoryError
  czy coś innego w przyszłości — jeden jawny warunek na wejściu do main()
  pokrywa oba te przypadki (is_file() jest False zarówno dla brakującego pliku,
  jak i dla katalogu podanego jako --input).
- EXIT_OK=0 / EXIT_USAGE_ERROR=2 jako nazwane stałe (nie magiczne liczby w
  kodzie main()), zgodnie z konwencją CLAUDE.md "reguły jako nazwane stałe,
  nie stringi/literały rozrzucone po kodzie" — użyta tu do kodów wyjścia
  zamiast do Reason (te już mają swój enum w models.py).
- Testy w test_cli.py wołają main(argv) bezpośrednio (funkcja czysta biorąca
  listę stringów i zwracająca int) zamiast uruchamiać `uv run feed-cleaner`
  przez subprocess — pytest robi to szybciej, a main() i tak jest jedynym
  entry pointem faktycznie używanym przez [project.scripts], więc test
  pośredni przez argparse-parse-i-wykonaj jest równoważny end-to-endowi.
  Ręczna weryfikacja entry pointu `uv run feed-cleaner --input ... --output-dir
  ...` (poza testami, zgodnie ze stop_condition zadania 7) zrobiona osobno w
  tej samej iteracji: exit 0, oba pliki wyjściowe utworzone, report.json z
  poprawnymi licznikami dla ręcznie przygotowanego mini-feedu (duplicate_sku +
  missing_sku + price_format), oraz exit 2 + czytelny stderr dla
  nieistniejącego pliku wejściowego.

Stop condition — cały feedback loop zielony + end-to-end zweryfikowany ręcznie:
1. uv run pytest — 79 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed
4. uv run feed-cleaner --input <fixture>.csv --output-dir <tmp> — exit 0,
   clean.parquet + report.json utworzone z poprawną zawartością
5. uv run feed-cleaner --input <nieistniejący>.csv --output-dir <tmp> —
   exit 2, czytelny komunikat na stderr, brak tracebacku

Do dokończenia: nic — zadanie 7 zamknięte. Następne w kolejce: zadanie 8
(polish — README.md z instrukcją instalacji/uruchomienia, finalny przegląd
pod kątem martwego kodu/TODO, potwierdzenie że cała sekwencja pytest+mypy+ruff
przechodzi razem).

## Zadanie 8 — Polish: README.md + finalny przegląd (DONE)

Uruchomione w trybie AFK (bezobsługowym) — bez pytań o zgodę, zgodnie z
punktem 7 sekcji HITL w CLAUDE.md.

Co zrobiłem:
- README.md: dopisana nowa sekcja "`feed-cleaner` — instalacja i uruchomienie"
  na samej górze pliku (przed opisem metodologii Ralpha) — dotychczasowy
  README opisywał WYŁĄCZNIE mechanikę pętli HITL/AFK, brakowało w nim tego,
  co PRD zadania 8 wprost wymagało: instrukcji `uv sync`, przykładowej
  komendy CLI i opisu plików wyjściowych (clean.parquet, report.json).
- Zweryfikowałem przykładową komendę z README faktycznym uruchomieniem
  (`uv run feed-cleaner --input data/raw/feed.csv --output-dir /tmp/...`) —
  pierwsza wersja wskazywała na nieistniejącą ścieżkę
  (`data/synthetic_feed.csv`), poprawiona na realną (`data/raw/feed.csv`,
  wygenerowaną przez `scripts/generate_synthetic_feed.py` przed startem
  pętli, zgodnie z CLAUDE.md). Po poprawce komenda faktycznie tworzy oba
  pliki wyjściowe i wypisuje skrót na stdout (Total: 36 | OK: 12 |
  Repaired: 12 | Rejected: 12).
- Finalny przegląd pod kątem martwego kodu/TODO: `grep -rn "TODO\|FIXME\|XXX"
  src/feed_cleaner tests` — brak wyników, nic do usunięcia.
- Nie ruszałem żadnego pliku w src/feed_cleaner/ ani tests/ — poza zakresem
  zadania 8 (files.include = tylko README.md, files.exclude jawnie wyklucza
  src/feed_cleaner/ i tests/), a finalny przegląd i tak nie znalazł nic do
  poprawy w kodzie.

Stop condition — cała sekwencja zielona w jednym przebiegu:
1. uv run pytest — 79 passed
2. uv run mypy src — Success, no issues
3. uv run ruff check . — All checks passed
4. README zawiera działający przykład komendy (zweryfikowany ręcznie, patrz
   wyżej).

Do dokończenia: nic — zadanie 8 zamknięte. Wszystkie zadania w PRD.json mają
teraz passes: true. Sprint zakończony.
